using System;

namespace RedWolfTests
{
    class Program
    {
        static void Main(string[] args)
        {
            //Запускает задачу по ООП:
            //OOPTask.Start();

            //Запускает задачу о нахождении совпадений в случайных массивах:
            //RandomArrayCoincidences.Start();

            //Запускает задачу о нахождении неотрицательных чисел в массиве:
            //RandomArrayNegative.Start();

            //Запускает задачу об удалении ненужных слов из текста:
            //UnnecessaryWords.Start();

            //Запускает задачу о поиске медианного значения массива:
            //MedianNumbers.Start();

            //Запускает задачу о выводе инверсии массива символов:
            //TextReverser.Start();

            //Запускает задачу о складывании слов:
            //TextTransformer.Start();
        }
    }
}
